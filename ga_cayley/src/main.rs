use geometric_algebra::{make_blades, Signature};

fn make_cayley_table(positive: usize, negative: usize, zero: usize) -> Result<(), String> {
    /* signature = Signature::new(positive, negative, zero)?;
    let blades = make_blades(signature);

    for i in 0..blades.len() {
        for j in 0..blades.len() {
            let product = blades[i] * blades[2];
        }
    }*/

    Err(String::from("Not implemented (yet)"))
}


fn main() {
    println!("Hello, world!");
}
